<div class="container">

	<div class="jumbotron">
  <h1 class="display-3">FireFinder微信小程序</h1>
  <h3 class="lead">分享/发现/创造</h3>
  <hr class="my-4">
  <p>来FireFinder找到想要的，让一切变得简单；来FireFinder分享，让生活变得美好！</p>
  <p class="lead">
    <a class="btn btn-danger btn-lg" href="/apps/new" role="button">分享小程序</a>
  </p>
</div>

	<div class="container">
	
		<div class="row text-center">
		<h1 class="current-category"><%= params[:category] %></h1>
			<% if @apps.count == 0 %>
				<h1>Hi!，该类别没有小程序哦！</h1>
			<% else %>
			<% @apps.each do |app| %>
				<div class="col-xs-6 col-md-3">

					<%= link_to app_path(app) do  %>
						
						<% if app.image.present? %>
							<%= image_tag(app.image.thumb.url, class: "thumbnail") %>
						<% else %>
							<%= image_tag("http://placehold.it/100x100&text=No Pic", class: "thumbnail") %>
						<% end %>
						
					<% end %>
					<div class="text-center">
						<h3><%= app.title %></h3>
					</div>
			</div>
			<% end %>
		<% end %>

	</div>

	<div class="text-center">
      <%= will_paginate(@apps, renderer: BootstrapPagination::Rails) %>
  </div>
	


