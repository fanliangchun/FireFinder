<div class="container">
	<h1><%= @app.title %></h1>
	<hr>
		<p>
			<%= simple_format(@app.description) %>
		</p>
		<div><%= @app.created_at.strftime("%Y-%m-%d") %></div>
	<div style="padding-top: 30px;">
		<%= link_to("返回列表", apps_path, class: "btn btn-xs btn-success") %>
	</div>
</div>
